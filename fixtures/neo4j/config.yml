Innmind\RestBundle\Tests\Controller\Bar:
    type: node
    id:
        uuid:
            type: string
            generator: { strategy: UUID }
    labels: [Bar]
    properties:
        name: string
